function addition(a, b) {
    a = parseInt(a);
    b = parseInt(b);
    var total = a + b;
    return total;
}

var addition = (a, b) => {};

[1, 2]
    .filter(function (item, index) {})

    [(1, 2)].filter((item, index) => {});
